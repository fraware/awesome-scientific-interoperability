name: Apply catalog v2.1 payload

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload upload in progress; application is temporarily disabled."
