name: Batch F source snapshot

on:
  push:
    branches:
      - agent/issue-44-batch-f-neuroscience-model-exchange
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Package exact tracked tree
        run: |
          python - <<'PY'
          import subprocess, zipfile
          files = subprocess.check_output(['git', 'ls-files'], text=True).splitlines()
          excluded = {'.github/workflows/batch-f-source-snapshot.yml'}
          with zipfile.ZipFile('batch-f-source-tree.zip', 'w', zipfile.ZIP_DEFLATED) as archive:
              for path in files:
                  if path not in excluded:
                      archive.write(path, path)
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: batch-f-source-tree
          path: batch-f-source-tree.zip
          retention-days: 1
